use algorithm::{
    data_structure,
    divide_and_conquer::matrix_multiply,
    sort::{bubble, heap, insertion, merge, select},
    utils::horner
};
fn main() {
    // insertion::example();

    // matrix_multiply::example();
    // select::example();
    // merge::example();
    // bubble::example();
    horner::example();
}
